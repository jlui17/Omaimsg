{
  "name": "omaimsg",
  "private": true,
  "type": "module",
  "workspaces": [
    "daemon",
    "mock"
  ],
  "scripts": {
    "bundle": "esbuild daemon/index.js --bundle --platform=node --format=cjs --outfile=daemon/dist/omaimsg-daemon.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
