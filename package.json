{
  "name": "omaimsg",
  "private": true,
  "type": "module",
  "workspaces": [
    "daemon",
    "mock"
  ],
  "scripts": {
    "bundle": "bun build daemon/index.js --target=node --format=cjs --outfile=daemon/dist/omaimsg-daemon.cjs"
  }
}
