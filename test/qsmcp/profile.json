{
  "config_dir": "stage/shell",
  "entry": "shell.qml",
  "env": {
    "HOME": "${PROFILE_DIR}/stage/home",
    "OMARCHY_PATH": "${PROFILE_DIR}/stage"
  },
  "backends": [
    {
      "name": "bluebubbles",
      "argv": [
        "node",
        "${PROFILE_DIR}/../server.js",
        "0"
      ],
      "ready": {
        "log": "listening on http"
      },
      "seed": {
        "from": "${PROFILE_DIR}/daemon-config.json",
        "to": "${WORK}/daemon-config.json"
      },
      "env_out": {
        "OMAIMSG_MOCK_CONFIG": "${WORK}/daemon-config.json"
      }
    },
    {
      "name": "daemon",
      "argv": [
        "node",
        "${PROFILE_DIR}/../../daemon/index.js"
      ],
      "ready": {
        "socket": "${XDG_RUNTIME_DIR}/io.omaimsg.sock"
      },
      "env_out": {
        "OMAIMSG_CONFIG": "${WORK}/daemon-config.json"
      }
    }
  ],
  "macros": []
}
